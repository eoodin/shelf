plugins {
  id "org.sonarqube" version "2.8"
}

sonarqube {
  properties {
    property "sonar.projectKey", "shelf"
    property "sonar.organization", "shelf-developers"
  }
}

group 'online.eoodin'
version '1.0-SNAPSHOT'

apply plugin: 'idea'

