import { Component } from '@angular/core';

@Component({
  moduleId: module.id,
  selector: 'shelf-server-app',
  templateUrl: 'shelf-server.component.html',
  styleUrls: ['shelf-server.component.css']
})
export class ShelfServerAppComponent {
  title = 'shelf-server works!';
}
